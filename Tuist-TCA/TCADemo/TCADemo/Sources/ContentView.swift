import SwiftUI

public struct ContentView: View {
    public var body: some View {
        VStack {
            Text("Hello, World!")
                .font(.largeTitle)
                .padding()
        }
    }
}

#Preview {
    ContentView()
}
